<h1>Here are all the stocks on your watchlist</h1>

<% @watchlists.each do |watchlist| %>
    <tr>
        <td>
            <%= watchlist.name %> -
        </td>
        <td>
            <%= watchlist.description %><br>
        </td>
        <td>
            <%= link_to 'Delete', user_watchlist_path(current_user, watchlist), :method => :delete %> |
            <%= link_to "Edit", edit_user_watchlist_path(current_user, watchlist) %><br>
        </td>
        <%end%>
        
            <%= link_to "Add a stock to the watchlist", new_user_watchlist_path(current_user) %><br>
            <%= link_to "Vew amounts added", user_watchlist_path(current_user, @watchlist) %>
            
